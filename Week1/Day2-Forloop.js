// Step 1: Create a variable to track the current number
let currentNumber;

// Step 2: Use a for loop from 1 to 25
for (currentNumber = 1; currentNumber <= 25; currentNumber++) {
  
  // Step 3: Check if the number is odd
  if (currentNumber % 2 !== 0) {
    
    // Step 4: Display the odd number
    console.log(currentNumber);
  }
}

